# Scatterplot_Matrix 优化分析报告

## 🔍 问题诊断

### 发现的问题：
1. **特征选择策略不当**：
   - 原策略：先基于t-test选前6个特征，再基于相关性筛选到4个
   - 问题：基于相关性筛选可能选择相关性高但类别分离度低的特征对
   - 结果：导致阳性和阴性在散点图中重叠严重，分离不明显

2. **可视化参数不够优化**：
   - 散点图size和alpha可能偏小
   - 缺少网格线辅助识别

---

## ✅ 优化方案

### 1. 特征选择优化
**改进前**：
- 基于t-test选前6个特征
- 再基于相关性筛选到4个特征

**改进后**：
- 基于t-test选前6个特征
- **直接选择类别分离度（Cohen's d）最高的前4个特征**
- Cohen's d = |mean_pos - mean_neg| / pooled_std
- 这是更准确的类别分离度指标

### 2. 可视化参数优化
**改进前**：
- alpha=0.7, s=30
- 无网格线

**改进后**：
- alpha=0.8, s=50（增大散点，提高可见性）
- edgecolors='white', linewidths=0.8（增强边缘对比）
- KDE曲线linewidth=3.0（更粗的曲线）
- 添加网格线（alpha=0.3，辅助识别）

---

## 📊 预期效果

优化后的Scatterplot_Matrix应该：
1. ✅ **类别分离更明显**：选择分离度最高的特征，阳性和阴性在特征空间中更容易区分
2. ✅ **可视化更清晰**：更大的散点、更粗的KDE曲线、网格线辅助
3. ✅ **统计更严谨**：使用Cohen's d作为分离度指标，比t-stat更准确

---

## 🎯 如果仍然分离不明显

如果优化后仍然分离不明显，可能的原因：
1. **数据本身重叠严重**：特征本身对类别区分能力有限
2. **需要降维可视化**：考虑使用t-SNE或UMAP进行非线性降维
3. **特征工程不足**：可能需要更好的特征提取或选择方法

**建议**：
- 如果分离度仍然不够，可以考虑：
  - 使用PCA + 前2-3个主成分绘制散点图
  - 使用t-SNE/UMAP进行非线性降维
  - 只选择分离度最高的2-3个特征（而不是4个）

---

**优化时间**: 2025-01-27

