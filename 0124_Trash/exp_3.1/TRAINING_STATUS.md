# 训练状态

## ✅ 训练已重启

**启动时间**: 2026-01-20 21:10:53  
**进程PID**: 3949497  
**GPU**: cuda:0 (使用率: 100%)  
**日志文件**: `logs/train_bio_cot_v3_20260120_211053.log`

## 📊 当前状态

### Epoch 1 结果

- **对齐损失 (Alignment Loss)**: 4.059
  - 注意：比之前的3.87略高，这是因为新增了L2辅助损失
  - 包含：InfoNCE损失 + 0.1 * L2距离损失
  
- **对齐召回率 (Recall@1)**:
  - 训练集: 0.0160
  - 验证集: 0.0260
  
- **验证集其他指标**:
  - AUC: 0.6578
  - F1-Score: 0.1615
  - 准确率: 0.3274

## 🔍 修复效果评估

### 预期改进时间线

- **Epoch 1-2**: 对齐损失可能略高（因为新增L2损失），召回率仍低（正常）
- **Epoch 3-5**: 对齐损失应开始下降（< 3.5），召回率应开始上升（> 0.05）
- **Epoch 5-10**: 对齐损失应显著下降（< 2.0），召回率应快速上升（> 0.3）
- **Epoch 10+**: 对齐损失应稳定在0.5-1.5，召回率应达到0.7-0.9

### 关键监控指标

训练日志中会显示以下指标：

1. **L_align**: 总对齐损失（InfoNCE + L2）
2. **L_align_ce**: InfoNCE损失（主损失）
3. **L_align_l2**: L2距离损失（辅助损失）
4. **align_cosine_sim**: 平均余弦相似度（应该接近1.0）
5. **Recall@1**: 对齐召回率（应该快速上升）

## 📈 如何判断修复是否成功

### 成功标志（Epoch 5后）

- ✅ 对齐损失 < 2.0
- ✅ 对齐召回率 > 0.3
- ✅ 平均余弦相似度 > 0.7

### 如果仍然失败（Epoch 10后）

如果Epoch 10后Recall@1仍然 < 0.3，可能需要：

1. **检查数据质量**：确保 `note_embeds` 不是全零或全相同
2. **增加对齐损失权重**：将 `lambda_align` 从0.5提升到1.0
3. **调整学习率**：为对齐模块使用更大的学习率
4. **使用监督对比学习**：如果数据有标签，使用标签信息组织正负对

## 🔄 实时监控

```bash
# 查看实时日志
tail -f logs/train_bio_cot_v3_20260120_211053.log

# 查看对齐相关指标
grep -E "对齐损失|对齐召回率|align_cosine" logs/train_bio_cot_v3_20260120_211053.log | tail -20

# 查看训练进程
ps aux | grep train_bio_cot_v3.py | grep -v grep
```

## 📝 修复内容回顾

1. ✅ 深度投影头（2层MLP + GELU + Dropout）
2. ✅ 共享语义空间投影
3. ✅ 辅助L2损失
4. ✅ 温度系数优化
5. ✅ 对齐损失权重提升（0.1 → 0.5）

