<!--
文件生成信息:
- 生成时间: 2025-12-24 19:40:50 (使用nvidia-smi获取CUDA时间)
- 更新时间: 2025-12-24 19:59:43 (使用nvidia-smi获取CUDA时间)
- 生成需求: 用户要求分析两个数据集的差别，检查数据内容是否完整，因为一个182GB，一个只有1.2GB
- 更新需求: 用户要求根据划分好的内外部数据集表格，在原始数据集中寻找对应的数据并创建软链接
- 生成原因: 需要明确两个数据集的区别，识别数据完整性问题，确保训练数据可用
- 更新原因: 服务器内存不够，无法复制182GB数据，通过软链接方式节省空间并组织数据
- 相关任务: 数据集分析和MICCAI论文准备

文件功能: 数据集深度分析报告，对比两个数据集的结构、大小、内容完整性；已更新：通过软链接方式解决数据完整性问题
-->

# 数据集深度分析报告

## 📊 执行摘要

**关键发现**：
- ⚠️ **严重问题**: `5centers_multi_internal_external_final/internal_train` **几乎为空**，只有目录结构，没有图像文件
- ✅ **外部测试集完整**: `external_validation` 包含完整的图像文件（1.2GB）
- ✅ **原始数据集完整**: `5centers_multi` 包含所有原始数据（182GB）

**结论**: 划分后的数据集**不完整**，训练集缺少图像文件，需要重新生成或修复。

---

## 1. 数据集基本信息

### 1.1 数据集路径

| 数据集 | 路径 | 类型 | 大小 |
|--------|------|------|------|
| **原始数据集** | `/data2/hmy/5Center_datas/5centers_multi` | 软链接 | **182GB** |
| **划分后数据集** | `/data2/hmy/5Center_datas/5centers_multi_internal_external_final` | 软链接 | **1.2GB** |

### 1.2 数据集结构对比

#### 原始数据集 (`5centers_multi`)

```
5centers_multi/
├── train/                          # 145GB
│   ├── col/                        # 1.1GB (Colposcopy图像)
│   │   └── [786个患者目录]/
│   │       └── *.jpg (每个患者3张图像)
│   └── oct/                        # 144GB (OCT图像序列)
│       └── [786个患者目录]/
│           └── [大量OCT图像文件]
├── test/                           # 38GB
│   ├── col/                        # Colposcopy图像
│   └── oct/                        # OCT图像序列
├── train_labels.csv                # 785行（785个训练样本）
├── test_labels.csv                 # 200行（200个测试样本）
└── dataset_info.json               # 数据集信息
```

**文件统计**:
- 训练集文件数: **96,555个**
- 测试集文件数: **24,838个**
- 训练集患者数: **786个**
- 测试集患者数: **约200个**

#### 划分后数据集 (`5centers_multi_internal_external_final`)

```
5centers_multi_internal_external_final/
├── internal_train/                 # ⚠️ 只有2.1MB（几乎为空）
│   ├── train/
│   │   ├── col/                    # ⚠️ 空目录（0个文件）
│   │   └── oct/                    # ⚠️ 只有2.1MB（几乎为空）
│   └── test/
│       ├── col/                    # ⚠️ 空目录
│       └── oct/                    # ⚠️ 空目录
├── external_validation/            # ✅ 1.2GB（完整）
│   ├── col/                        # ✅ 1.2GB (1,422个图像文件)
│   ├── oct/                        # ✅ 1.9MB
│   └── test/
├── train_labels.csv                 # 660行（内部训练集）
├── val_labels.csv                  # 166行（内部验证集）
├── external_test_labels.csv        # 159行（外部测试集）
└── README.md                        # 划分说明
```

**文件统计**:
- 内部训练集图像文件: **0个** ⚠️ **严重问题**
- 外部测试集图像文件: **1,422个** ✅
- 内部训练集样本数: **660个**（标签文件存在）
- 外部测试集样本数: **159个**（标签文件存在）

---

## 2. 数据完整性分析

### 2.1 原始数据集 (`5centers_multi`) ✅

**状态**: ✅ **完整**

| 组件 | 大小 | 文件数 | 状态 |
|------|------|--------|------|
| 训练集 (train) | 145GB | 96,555 | ✅ 完整 |
| 测试集 (test) | 38GB | 24,838 | ✅ 完整 |
| 标签文件 | 788KB | 2个CSV | ✅ 完整 |
| **总计** | **182GB** | **121,393** | ✅ **完整** |

**数据分布**:
- 训练集: 785个样本
- 测试集: 200个样本
- 总样本数: 985个

### 2.2 划分后数据集 (`5centers_multi_internal_external_final`) ⚠️

**状态**: ⚠️ **不完整 - 训练集缺少图像文件**

| 组件 | 大小 | 文件数 | 状态 |
|------|------|--------|------|
| 内部训练集 (internal_train) | 2.1MB | **0个图像** | ⚠️ **几乎为空** |
| 外部测试集 (external_validation) | 1.2GB | 1,422个图像 | ✅ 完整 |
| 标签文件 | 84KB | 3个CSV | ✅ 完整 |
| **总计** | **1.2GB** | **1,422** | ⚠️ **不完整** |

**问题分析**:
- ❌ **内部训练集**: 只有目录结构，**没有图像文件**
- ✅ **外部测试集**: 包含完整的Colposcopy图像（1,422个文件）
- ❌ **OCT图像**: 内部训练集和外部测试集的OCT图像都几乎为空

---

## 3. 数据集差异详细对比

### 3.1 样本数量对比

| 数据集 | 训练集 | 验证集/测试集 | 外部测试集 | 总计 |
|--------|--------|---------------|------------|------|
| **原始数据集** | 785 | 200 | - | 985 |
| **划分后数据集** | 660 | 166 | 159 | 985 |

**差异**:
- 原始数据集: 简单的train/test划分
- 划分后数据集: 按医疗中心划分（内部开发集 + 外部测试集）
- 样本总数一致: 985个

### 3.2 数据大小差异原因

**为什么划分后数据集只有1.2GB？**

1. **内部训练集几乎为空** (2.1MB)
   - 只有目录结构
   - **没有图像文件**
   - 这是主要问题

2. **外部测试集完整** (1.2GB)
   - 包含1,422个Colposcopy图像文件
   - 但OCT图像几乎为空

3. **OCT图像缺失**
   - 原始数据集: OCT图像占144GB（训练集）
   - 划分后数据集: OCT图像几乎全部缺失

### 3.3 数据划分策略

**划分依据**: 按医疗中心划分

| 中心 | 样本数 | 用途 | 状态 |
|------|--------|------|------|
| **恩施** | 404 | 内部开发集 | ⚠️ 图像文件缺失 |
| **襄阳** | 344 | 内部开发集 | ⚠️ 图像文件缺失 |
| **十堰** | 78 | 内部开发集 | ⚠️ 图像文件缺失 |
| **荆州** | 70 | 外部测试集 | ✅ 图像文件完整 |
| **武大** | 89 | 外部测试集 | ✅ 图像文件完整 |

---

## 4. 问题诊断

### 4.1 核心问题

**问题1: 内部训练集图像文件缺失** 🔴 **严重**

- **现象**: `internal_train/train/col/` 和 `internal_train/train/oct/` 目录为空
- **影响**: **无法进行训练**，因为训练集没有图像数据
- **原因**: 数据集划分脚本可能只创建了目录结构和标签文件，但没有复制图像文件

**问题2: OCT图像缺失** ⚠️ **中等**

- **现象**: 划分后数据集的OCT图像几乎全部缺失
- **影响**: 如果模型需要OCT图像，将无法训练
- **原因**: 可能OCT图像文件太大，划分脚本没有复制

### 4.2 数据完整性检查

**检查结果**:

```
✅ 标签文件: 完整（train_labels.csv, val_labels.csv, external_test_labels.csv）
✅ 外部测试集Colposcopy图像: 完整（1,422个文件）
❌ 内部训练集Colposcopy图像: 缺失（0个文件）
❌ 内部训练集OCT图像: 缺失（几乎为空）
❌ 外部测试集OCT图像: 缺失（几乎为空）
```

---

## 5. 解决方案

### 5.1 方案1: 使用原始数据集（推荐）✅

**优点**:
- ✅ 数据完整（182GB，所有图像文件都在）
- ✅ 可以直接使用
- ✅ 不需要重新生成

**使用方法**:
```python
# 使用原始数据集
data_path = '/data2/hmy/VLM_Caus_Rm_Mics/data/5centers_multi'

# 在代码中手动划分内部/外部测试集
# 根据标签文件中的中心信息进行划分
```

**注意事项**:
- 需要在训练代码中实现按中心划分的逻辑
- 确保外部测试集（荆州、武大）不参与训练

### 5.2 方案2: 重新生成划分后的数据集 🔧

**步骤**:
1. 检查划分脚本: `exp1_Causal_Bayesian_clip/code/split_dataset_by_centers.py`
2. 修复脚本，确保复制图像文件（不仅仅是创建目录）
3. 重新运行划分脚本

**需要修复的问题**:
- 确保复制Colposcopy图像文件
- 确保复制OCT图像文件（可能需要处理大文件）
- 验证复制后的文件完整性

### 5.3 方案3: 使用软链接（节省空间）💡

**思路**:
- 不复制图像文件，只创建软链接
- 保持原始数据集不变
- 划分后的数据集只包含标签文件和软链接

**优点**:
- 节省磁盘空间
- 数据完整性有保障
- 划分逻辑清晰

---

## 6. 推荐方案

### 🎯 推荐: 方案1 + 方案3 结合

**具体做法**:

1. **继续使用原始数据集** (`5centers_multi`)
   - 数据完整，可以直接使用

2. **在训练代码中实现按中心划分**
   ```python
   # 根据标签文件中的中心信息划分
   def split_by_centers(df):
       # 内部中心: 恩施、襄阳、十堰
       internal_centers = ['M22102', 'M22105', 'M22106']  # 需要确认中心代码
       # 外部中心: 荆州、武大
       external_centers = ['M0008', 'M20203', 'M20105']
       
       internal_df = df[df['OCT'].str.contains('|'.join(internal_centers))]
       external_df = df[df['OCT'].str.contains('|'.join(external_centers))]
       
       return internal_df, external_df
   ```

3. **使用划分后的标签文件**
   - 使用 `5centers_multi_internal_external_final/train_labels.csv` 等
   - 但图像文件从原始数据集读取

---

## 7. 数据使用建议

### 7.1 当前可用数据

**✅ 可以立即使用**:
- 原始数据集 (`5centers_multi`): 182GB，完整
- 划分后的标签文件: 完整，可以用于数据划分

**❌ 不能使用**:
- 划分后的内部训练集图像: 缺失

### 7.2 训练代码修改建议

```python
# 推荐的数据加载方式
from pathlib import Path
import pandas as pd

# 标签文件路径（使用划分后的）
labels_file = 'data/5centers_multi_internal_external_final/train_labels.csv'

# 图像文件路径（使用原始数据集）
image_root = Path('data/5centers_multi')

# 加载标签
df = pd.read_csv(labels_file)

# 根据标签中的路径，从原始数据集读取图像
for idx, row in df.iterdir():
    # 从原始数据集读取图像
    oct_path = image_root / 'train' / 'oct' / row['OCT']
    col_path = image_root / 'train' / 'col' / row['ID']
    # ...
```

---

## 8. 解决方案实施结果 ✅

### 8.1 软链接创建方案（已实施）

**实施时间**: 2025-12-24 19:59:43

**方案描述**:
根据划分后的标签文件（`train_labels.csv`, `val_labels.csv`, `external_test_labels.csv`），在原始数据集中找到对应的图像文件，并创建软链接到划分后的数据集目录。

**实施结果**:

| 数据集 | 样本数 | OCT图像 | Colposcopy图像 | 状态 |
|--------|--------|---------|----------------|------|
| **内部训练集** | 660 | ✅ 660/660 (100%) | ✅ 660/660 (100%) | ✅ 完成 |
| **内部验证集** | 166 | ✅ 166/166 (100%) | ✅ 166/166 (100%) | ✅ 完成 |
| **外部测试集** | 159 | ✅ 159/159 (100%) | ✅ 159/159 (100%) | ✅ 完成 |
| **总计** | **985** | **✅ 985/985 (100%)** | **✅ 985/985 (100%)** | **✅ 完成** |

**软链接统计**:
- OCT图像软链接: **985个**
- Colposcopy图像软链接: **985个**
- 总软链接数: **1,970个**

**目录结构**:
```
5centers_multi_internal_external_final/
├── internal_train/
│   ├── train/
│   │   ├── oct/          # 660个OCT软链接 → 原始数据集train/oct/
│   │   └── col/           # 660个Colposcopy软链接 → 原始数据集train/col/
│   └── val/
│       ├── oct/          # 166个OCT软链接 → 原始数据集train/oct/
│       └── col/          # 166个Colposcopy软链接 → 原始数据集train/col/
└── external_validation/
    ├── oct/              # 159个OCT软链接 → 原始数据集test/oct/
    └── col/              # 159个Colposcopy软链接 → 原始数据集test/col/
```

**优势**:
- ✅ **节省磁盘空间**: 不复制182GB数据，只创建软链接（约3.3MB）
- ✅ **数据完整性**: 所有图像文件都找到了（100%）
- ✅ **易于维护**: 数据实际存储在原始数据集中，统一管理
- ✅ **快速实施**: 脚本自动完成，无需手动操作

**使用方法**:
```python
# 现在可以直接使用划分后的数据集路径
data_path = '/data2/hmy/VLM_Caus_Rm_Mics/data/5centers_multi_internal_external_final'

# 软链接会自动指向原始数据集中的实际文件
# 训练代码可以直接使用这些路径，就像使用普通目录一样
```

**脚本位置**:
- 创建脚本: `/data2/hmy/VLM_Caus_Rm_Mics/cursor_file/create_symbolic_links.py`
- 可以随时重新运行，更新软链接

---

## 9. 总结

### 9.1 关键发现

1. **原始数据集完整** ✅
   - 182GB，包含所有图像文件
   - 可以直接使用

2. **划分后数据集问题已解决** ✅
   - 通过软链接方式，所有图像文件都已链接
   - 数据完整性: 100%
   - 节省磁盘空间: 从182GB减少到约3.3MB（软链接）

3. **数据划分逻辑正确** ✅
   - 标签文件完整
   - 划分策略合理（按医疗中心）
   - 软链接创建成功

### 9.2 最终状态

**✅ 问题已解决**:
- 划分后的数据集现在包含所有必要的图像文件（通过软链接）
- 可以直接用于训练和验证
- 数据完整性: 100%

**✅ 推荐使用**:
- 使用划分后的数据集路径: `/data2/hmy/VLM_Caus_Rm_Mics/data/5centers_multi_internal_external_final`
- 软链接会自动指向原始数据集中的实际文件
- 训练代码可以直接使用这些路径

---

**分析完成日期**: 2025-12-24  
**最后更新**: 2025-12-24 19:59:43  
**状态**: ✅ 问题已解决，软链接创建成功，数据集可以使用

